const file = require('./util/fileUtil');
const map = require('./util/mapUtil');
const str = require('./util/stringUtil');
const random = require('./util/randomUtil');
const set = require('./util/setUtil');
const object = require('./util/objectUtil');
const proto = require('./util/protoUtils');
const item = require('./util/itemUtils');
const time = require('./util/timeUtil');
const mail = require('./util/mailUtil');
const array = require('./util/arrayUtil');
const weight = require('./util/weightUtil');
const format = require('./util/formatUtil');
const encode = require('./util/encodeUtil');
const color = require('./util/colorUtil');

module.exports = {
    file,
    map,
    str,
    random,
    set,
    object,
    proto,
    item,
    time,
    mail,
    array,
    weight,
    format,
    encode,
    color,
};
